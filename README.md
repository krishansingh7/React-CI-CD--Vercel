# My React App

Built with React + Vite. Auto-deployed to Vercel via GitHub Actions.

## Local Development

npm install
npm run dev        # starts at http://localhost:5173

## Build

npm run build      # outputs to /dist
npm run preview    # preview the production build locally